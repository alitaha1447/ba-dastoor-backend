const BranchImage = require("../models/branchImagesModel")
const uploadToCloudinary = require("../../utils/cloudinaryUpload")

module.exports = {
    uploadBranchImage: async (req, res) => {
        const { } = req.body;
    }
}